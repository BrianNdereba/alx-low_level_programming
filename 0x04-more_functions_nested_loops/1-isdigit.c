#include "main.h"

/**
 * _isdigit - check whether character is digit
 * @c: character to be checked
 * Return:1 for digit, else 0.
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	return (1);
}
